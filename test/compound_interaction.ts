import { expect } from "chai";
import { ethers } from "hardhat";

describe("Compound Interaction", function () {
    it("Balance of owner in cdai contract should be > 0", async function () {
        
    });
});